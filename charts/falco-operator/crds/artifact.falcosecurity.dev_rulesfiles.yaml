---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: rulesfiles.artifact.falcosecurity.dev
spec:
  group: artifact.falcosecurity.dev
  names:
    categories:
    - artifacts
    kind: Rulesfile
    listKind: RulesfileList
    plural: rulesfiles
    singular: rulesfile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The priority of the rulesfile
      jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .status.conditions[?(@.type == 'Programmed')].status
      name: Programmed
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Rulesfile is the Schema for the rulesfiles API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RulesfileSpec defines the desired state of Rulesfile.
            properties:
              configMapRef:
                description: ConfigMapRef specifies a reference to a ConfigMap containing
                  the rules.
                properties:
                  name:
                    description: Name is the name of the ConfigMap.
                    type: string
                required:
                - name
                type: object
              inlineRules:
                description: InlineRules specifies the rules as a structured object
                  in YAML format.
                x-kubernetes-preserve-unknown-fields: true
              ociArtifact:
                description: OCIArtifact specifies the reference to an OCI artifact.
                properties:
                  image:
                    description: Image specifies the OCI image coordinates.
                    properties:
                      repository:
                        description: Repository is the OCI repository path (e.g. "falcosecurity/rules/falco-rules").
                        type: string
                      tag:
                        default: latest
                        description: Tag is the image tag or digest (e.g. "latest"
                          or "sha256:abc...").
                        type: string
                    required:
                    - repository
                    type: object
                  registry:
                    description: Registry contains inline registry configuration for
                      authentication, TLS, and hostname.
                    properties:
                      auth:
                        description: Auth contains authentication configuration.
                        properties:
                          secretRef:
                            description: SecretRef references a Secret containing
                              registry credentials.
                            properties:
                              name:
                                description: Name is the name of the Secret containing
                                  credentials.
                                type: string
                            required:
                            - name
                            type: object
                        type: object
                      name:
                        description: Name is the registry hostname (e.g. "ghcr.io").
                        type: string
                      plainHTTP:
                        description: |-
                          PlainHTTP allows connections to registries over plain HTTP (no TLS).
                          Mutually exclusive with tls.
                        type: boolean
                      tls:
                        description: |-
                          TLS contains TLS transport configuration.
                          Mutually exclusive with plainHTTP.
                        properties:
                          insecureSkipVerify:
                            description: InsecureSkipVerify disables TLS certificate
                              verification.
                            type: boolean
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: plainHTTP and tls are mutually exclusive
                      rule: '!(has(self.plainHTTP) && self.plainHTTP && has(self.tls))'
                required:
                - image
                type: object
              priority:
                default: 50
                description: |-
                  Priority specifies the priority of the rulesfile.
                  The higher the value, the higher the priority.
                format: int32
                maximum: 99
                minimum: 0
                type: integer
              selector:
                description: Selector is used to select the nodes where the rulesfile
                  should be applied.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            type: object
          status:
            default:
              conditions:
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for controller
                reason: Pending
                status: Unknown
                type: Programmed
            description: RulesfileStatus defines the observed state of Rulesfile.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the Rulesfile's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
